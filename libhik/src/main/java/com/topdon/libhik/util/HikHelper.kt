package com. topdon. libhik. util import android. content. Context import androidx. activity. ComponentActivity import androidx. annotation. IntRange import androidx. fragment. app. Fragment import androidx. lifecycle. DefaultLifecycleObserver import androidx. lifecycle. LifecycleOwner import androidx. lifecycle. lifecycleScope import com. elvishew. xlog. XLog import com. hcusbsdk. Interface. FStreamCallBack import com. hcusbsdk. Interface. JavaInterface import com. hcusbsdk. Interface. USB_FRAME_INFO import kotlinx. coroutines. CoroutineScope import kotlinx. coroutines. Dispatchers import kotlinx. coroutines. Job import kotlinx. coroutines. delay import kotlinx. coroutines. launch import kotlinx. coroutines. withContext import java. util. Arrays / *  *  * implement, . *  * set to: * - [setFrameListener] Settings YUV * - [setTempListener] Settingstemperature *  * set to set to: * - [onReadyListener] *  * set to: * - [onTimeoutListener] *  * Error: * - [init] * - [startStream] * - [stopStream] stop * - [release] *  * [ComponentActivity], * [bind], 4 *  * , , *  * Created by LCG on 2024/11/19. * / object HikHelper { / *  *  * Id. * / private var userId: Int = JavaInterface. USB_INVALID_USER_ID / *  *  * Id, forstop. * / private var callbackId: Int = JavaInterface. USB_INVALID_CHANNEL / *  *  * Job * / private var timeoutJob: Job? = null / *  *  * . * / private val streamCallBack = MyFStreamCallBack () / *  *  * Activity, , stop, operation. * / fun bind (activity: ComponentActivity) { init (activity) activity. lifecycle. addObserver (object: DefaultLifecycleObserver { override fun onResume (owner: LifecycleOwner) { owner. lifecycleScope. launch { startStream () } } override fun onStop (owner: LifecycleOwner) { stopStream () } override fun onDestroy (owner: LifecycleOwner) { release () } }) } / *  *  * Fragment, , stop, operation. * / fun bind (fragment: Fragment) { init (fragment. requireContext () ) fragment. lifecycle. addObserver (object: DefaultLifecycleObserver { override fun onResume (owner: LifecycleOwner) { owner. lifecycleScope. launch { startStream () } } override fun onStop (owner: LifecycleOwner) { stopStream () } override fun onDestroy (owner: LifecycleOwner) { release () } }) } / *  *  * , for USB_Cleanup * / private var hasInit = false / *  *  * , . * @return true - false - * / fun init (context: Context) : Boolean { if (userId != JavaInterface. USB_INVALID_USER_ID) { // return true } // if (!hasInit) { if (!HikCmdUtil. init () ) { // return false } hasInit = true } // set to HikCmdUtil. setLogPath (context. getExternalFilesDir ("hkLog") ) // userId = HikCmdUtil. login (context) return userId != JavaInterface. USB_INVALID_USER_ID } / *  *  *  * / @Volatile private var wantCheckTimeout = false / *  *  * . * / suspend fun startStream () { if (callbackId == JavaInterface. USB_INVALID_CHANNEL) { callbackId = HikCmdUtil. startStream (userId, streamCallBack) wantCheckTimeout = true timeoutJob = CoroutineScope (Dispatchers. Main) . launch { while (wantCheckTimeout) { delay (5000) // 5 if (System. currentTimeMillis () - streamCallBack. beforeFrameTime > 5000) { wantCheckTimeout = false onTimeoutListener?. invoke () } } } onReadyListener?. invoke () } } / *  *  * stop, , YUV temperature null. * / fun stopStream () { if (callbackId != JavaInterface. USB_INVALID_CHANNEL) { timeoutJob?. cancel () HikCmdUtil. removeStreamCallback (userId, callbackId) callbackId = JavaInterface. USB_INVALID_CHANNEL wantCheckTimeout = false } } / *  *  * . * / fun release () { if (userId != JavaInterface. USB_INVALID_USER_ID) { // JavaInterface. getInstance () . USB_Logout (userId) userId = JavaInterface. USB_INVALID_USER_ID } if (hasInit) { JavaInterface. getInstance () . USB_Cleanup () hasInit = false } streamCallBack. onTempListener = null streamCallBack. onFrameListener = null onTimeoutListener = null } / *  *  * set to, , line measurement measurement ! * / fun setFrameListener (listener: ( (ByteArray, ByteArray) -> Unit) ) { streamCallBack. onFrameListener = listener } / *  *  * Settingstemperature, , line measurement measurement ! * / fun setTempListener (listener: ( (ByteArray) -> Unit) ) { streamCallBack. onTempListener = listener } / *  *  * , eventlistener. * / var onTimeoutListener: ( () -> Unit) ? = null / *  *  * , , set. * / var onReadyListener: ( () -> Unit) ? = null / *  *  * (YUV+temperature) * / fun copyFrameData () : ByteArray = streamCallBack. copyFrameArray () private class MyFStreamCallBack: FStreamCallBack { / *  *  * YUV, YUY2 * / private val yuvArray = ByteArray (256 * 192 * 2) / *  *  * temperature * / private val tempArray = ByteArray (256 * 192 * 2) / *  *  * temperature. * / @Volatile var onTempListener: ( (ByteArray) -> Unit) ? = null / *  *  * (YUY2+temperature) . * / @Volatile var onFrameListener: ( (ByteArray, ByteArray) -> Unit) ? = null / *  *  *  * / @Volatile var beforeFrameTime: Long = 0 / *  *  * (YUV+temperature) * / fun copyFrameArray () : ByteArray = synchronized (this) { val frameArray = ByteArray (256 * 192 * 4) System. arraycopy (yuvArray, 0, frameArray, 0, yuvArray. size) System. arraycopy (tempArray, 0, frameArray, yuvArray. size, tempArray. size) frameArray } override fun invoke (handle: Int, frameInfo: USB_FRAME_INFO) { beforeFrameTime = System. currentTimeMillis () if (frameInfo. dwBufSize != 4640 + 256 * 192 * 4) { XLog. w ("! ${frameInfo. dwBufSize}") return } val dataArray: ByteArray = Arrays. copyOf (frameInfo. pBuf, frameInfo. dwBufSize) / * val frameHead = FrameHead (dataArray) XLog. d (frameHead. toString () ) for (i in frameHead. tempRuleList. indices) { XLog. v (frameHead. tempRuleList[i]. toString () ) XLog. v (dataArray. buildPrintStr (216 + i * 208, 88) ) } * / synchronized (this) { System. arraycopy (dataArray, dataArray. size - yuvArray. size, yuvArray, 0, yuvArray. size) for (i in tempArray. indices step 2) { val newValue = ( (dataArray[4640 + i]. toInt () and 0xff) or (dataArray[4640 + i + 1]. toInt () and 0xff shl 8) ) + 14272 tempArray[i] = (newValue and 0xff) . toByte () tempArray[i + 1] = (newValue shr 8 and 0xff) . toByte () } } onTempListener?. invoke (tempArray) onFrameListener?. invoke (yuvArray, tempArray) } } / *  *  * Error occurred. * / suspend fun initConfig () = withContext (Dispatchers. IO) { HikCmdUtil. initConfig (userId) } / *  *  * , , line measurement measurement line measurement * / suspend fun shutter () = withContext (Dispatchers. IO) { HikCmdUtil. shutter (userId) } / *  *  *  * / suspend fun setAutoShutter (isAutoShutter: Boolean) = withContext (Dispatchers. IO) { HikCmdUtil. setAutoShutter (userId, isAutoShutter) } / *  *  * set to, `[0, 100]` * / suspend fun setContrast (value: Int) = withContext (Dispatchers. IO) { HikCmdUtil. setContrast (userId, value) } / *  *  * , `[0, 100]` * / suspend fun setEnhanceLevel (value: Int) = withContext (Dispatchers. IO) { HikCmdUtil. setEnhanceLevel (userId, value) } / *  *  * set to * / suspend fun setMirror (rotateAngle: Int, isMirror: Boolean) = withContext (Dispatchers. IO) { HikCmdUtil. setMirror (userId, rotateAngle, isMirror) } / *  *  * set to * @param emissivity range `[1, 100]` * / suspend fun setEmissivity (emissivity: Int) = withContext (Dispatchers. IO) { HikCmdUtil. setEmissivity (userId, emissivity) } / *  *  * set to, cm * @param distance range `[30, 200]` * / suspend fun setDistance (distance: Int) = withContext (Dispatchers. IO) { HikCmdUtil. setDistance (userId, distance) } / *  *  * set to (-1) / (1) /height (0) level. * / suspend fun setTemperatureMode (@IntRange (-1, 1) mode: Int) = withContext (Dispatchers. IO) { HikCmdUtil. setTemperatureMode (userId, mode) } / *  *  * start * / suspend fun startCorrect () = withContext (Dispatchers. IO) { HikCmdUtil. startCorrect (userId) } }