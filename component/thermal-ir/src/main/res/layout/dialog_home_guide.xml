<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 低于 Android12 显示操作指引时模糊背景图片 -->
    <ImageView
        android:id="@+id/iv_blur_bg"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="@null"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <include
        android:id="@+id/cl_guide_1"
        layout="@layout/layout_home_guide_1" />
    <include
        android:id="@+id/cl_guide_2"
        layout="@layout/layout_home_guide_2"
        tools:visibility="gone" />
    <include
        android:id="@+id/cl_guide_3"
        layout="@layout/layout_home_guide_3"
        tools:visibility="gone"/>

</androidx.constraintlayout.widget.ConstraintLayout>